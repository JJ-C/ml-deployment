# Spark dependencies for Feast materialization
pyspark>=3.3.0
pyarrow>=10.0.0

# Cassandra Python driver (for direct writes)
cassandra-driver>=3.25.0

# Note: For production Spark-to-Cassandra connector, add the JAR to Spark classpath:
# spark-submit --packages com.datastax.spark:spark-cassandra-connector_2.12:3.4.0
